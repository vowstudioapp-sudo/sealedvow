{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        "drafts": {
          ".indexOn": ["persistenceStatus"],
          ".read": "auth != null && auth.uid == $uid",
          ".write": false
        }
      }
    }
  }
}
